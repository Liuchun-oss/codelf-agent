// 记忆文件模板与各节 token 预算。模板是 source of truth 的初始骨架；
// 注入与裁剪时按 ## 标题分节，结合下方预算控制总量。

export const MEMORY_TEMPLATE = `# 项目记忆
_本文件记录跨会话持久的项目级知识，所有会话均会加载。可手动编辑，只填写各节标题下的内容。_

## 项目背景
_这是什么项目？目标是什么？高层定位。_

(暂无)

## 规则
_用户明确要求、每个会话都必须遵守的硬约束。_

(暂无)

## 架构决策
_重要设计选择及其理由。对未来会话而言，“为什么”比“是什么”更重要。_

(暂无)

## 跨会话稳定知识
_反复验证、可长期复用的事实。_

(暂无)
`

export const NOTES_TEMPLATE = `# 会话草稿
_主代理的自由记事区。随手追加发现；后续由 checkpoint 整理。每条建议用 \`## [turn N]\` 起头，再写正文。_
`

/** MEMORY.md 各节 token 预算（注入/裁剪用）。键为 ## 标题文本。 */
export const MEMORY_SECTION_BUDGETS: Record<string, number> = {
  项目背景: 1000,
  规则: 2000,
  架构决策: 3000,
  跨会话稳定知识: 4000
}

/** 全局记忆整体注入预算上限。 */
export const GLOBAL_MEMORY_BUDGET = 3000

// ── 会话 checkpoint（11 字段结构化模板，阶段 2） ──────────────────────────
// writer 子代理按此结构原地更新；字段顺序与标题需稳定，便于增量更新与校验。

export const CHECKPOINT_TEMPLATE = `# 会话 checkpoint
_由 checkpoint writer 维护，结构在更新间保持稳定。只填写各节标题下的内容。_

## §1 当前意图
_用户最近一次明确请求，尽量逐字引用，不要改写。_

(暂无)

## §2 下一步动作
_由 §1 与当前状态推出的、单一且具体的下一步。_

(暂无)

## §3 本会话约束
_本会话特定的工作方式。项目级规则在 MEMORY.md，不要在此重复。_

(暂无)

## §4 任务树
_层级任务视图，标出当前焦点。_

(暂无)

## §5 当前工作
_本次 checkpoint 前正在做的事，写明涉及的文件路径与代码位置。_

(暂无)

## §6 涉及文件
_正在读取或修改的文件，每条附一句用途。_

(暂无)

## §7 跨任务发现
_本会话学到、可能对未来任务有用的事实。稳定者可晋升到 MEMORY.md。_

(暂无)

## §8 错误与修复
_本会话遇到的错误及解决方式，最新在前。_

(暂无)

## §9 运行时状态
_分支、未提交文件、运行中进程、临时产物等最易变信息。_

(暂无)

## §10 设计决策
_讨论得出、暂无代码产物的决策与理由（“为什么这样做”）。稳定者可晋升到 MEMORY.md。_

(暂无)

## §11 开放笔记
_不属于以上各节的杂项、引用、未决问题。_

(暂无)
`

/** checkpoint.md 各节 token 预算。键为不含 "## " 前缀的标题文本。 */
export const CHECKPOINT_SECTION_BUDGETS: Record<string, number> = {
  '§1 当前意图': 500,
  '§2 下一步动作': 1000,
  '§3 本会话约束': 800,
  '§4 任务树': 1000,
  '§5 当前工作': 2000,
  '§6 涉及文件': 1500,
  '§7 跨任务发现': 2000,
  '§8 错误与修复': 1500,
  '§9 运行时状态': 1000,
  '§10 设计决策': 3000,
  '§11 开放笔记': 800
}
